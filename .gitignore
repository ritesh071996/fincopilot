# --- Secrets ---
.env

# --- Python ---
.venv/
venv/
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# --- Data artifacts ---
# Corpus PDFs ARE committed (public RBI docs) so the cloud app can build its
# index on first run. The vector index itself is rebuilt at startup, so ignore it.
data/vectorstore/*
!data/**/.gitkeep

# --- IDE / OS ---
.vscode/
.idea/
.DS_Store
Thumbs.db
