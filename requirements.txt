# FinCopilot dependencies (Python 3.13)
# Loose lower bounds so pip resolves compatible current versions.
# Install:  pip install -r requirements.txt

# --- Web backend ---
fastapi>=0.115
uvicorn[standard]>=0.30
pydantic>=2.7
pydantic-settings>=2.3
python-dotenv>=1.0

# --- Orchestration (LangChain / LangGraph) ---
langchain>=0.3
langchain-community>=0.3
langchain-openai>=0.2
langgraph>=0.2

# --- Retrieval / RAG ---
chromadb>=0.5               # local embedded vector DB (no server)
pypdf>=4.2                  # read RBI PDFs
sentence-transformers>=3.0  # local embedding model engine (no Api-Key)
langchain-huggingface>=0.1  # LangChain wrapper for sentence-transformers
langchain-chroma>=0.1       # Langchain wrapper for chroma vector store

# --- LLM Gateway ---
litellm>=1.40          # one interface for many model providers

# --- UI ---
streamlit>=1.36

# --- Eval / utilities ---
pyyaml>=6.0
httpx>=0.27
